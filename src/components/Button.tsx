import styled from "styled-components";

const ButtonContainer = styled.button`
  width: 54px;
  background: white;
  border: none;
  color: #1ea1f7;
  cursor: pointer;
`;

const Button = () => {
  return <ButtonContainer>.GG</ButtonContainer>;
};

export default Button;
